/** Auto-advance delay after a turn completes (`LiveDebate.tsx`). */
export const AUTO_ADVANCE_MS = 2000;

/** SSE reconnect base delay in ms (`LiveDebate.tsx`). */
export const SSE_RECONNECT_BASE_MS = 1000;

/** SSE reconnect max backoff in ms (`LiveDebate.tsx`). */
export const SSE_RECONNECT_MAX_MS = 30_000;
